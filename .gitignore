
# Terraform state files (Can delete them if the backend is in S3)
terraform/terraform.tfstate  
terraform/terraform.tfstate.backup

# Terraform working directory
terraform/.terraform/    

# Lock file (optional)
.terraform.lock.hcl

# Temporary or build artifacts
terraform/SES_lambda.zip
terraform/crypto_api.zip
terraform/viewer_count.zip
terraform/CV.pdf 

# Sensitive variable files
terraform/terraform.tfvars

# System files
notes.txt
terraform/CV-SamuelAlbershtein

# Never commit terraform.tfstate or terraform.tfvars: These can contain secrets or sensitive information.
# If you accidentally committed sensitive files (e.g., terraform.tfstate), remove them from your Git history using a tool like git filter-repo or git rm --cached.